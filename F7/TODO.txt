WM:
	Gradient background for sky on world map
	Track steps
	Encounter tables

Background load razor engine since initial load takes so long!
	-Means splash / load screen will need to *not* be a layout, and code it manually

Compressed/single file saves